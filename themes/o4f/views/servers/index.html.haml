.module
  .bar Servers
  %table{:cellpadding => 0, :cellspacing => 0}
    %tr
      %th Name
      %th Address
      - if can? :manage, Server
        %th{:colspan => 2} Options  
    - @servers.each do |s|
      %tr
        %td= s.title
        %td= s.address
        - if can? :manage, Server
          %td= link_to "Edit", [:edit, s]
          %td= link_to "Remove", s, :method => :delete, :confirm => "Are you absolutely sure?"


- if can? :manage, Server
  .links
    = link_to "New Server", new_resource_path
