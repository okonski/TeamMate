.module
  .bar Servers
  %table{:cellpadding => 0, :cellspacing => 0}
    %tr
      %th Name
      %th Address
      %th Type
      - if can? :manage, Server
        %th Options  
    - @servers.each do |s|
      %tr
        %td= link_to s.title, resource_path(s)
        %td= s.address
        %td= s.title
        - if can? :manage, Server
          %td destroy


- if can? :manage, Server
  .links
    = link_to "New Server", new_resource_path
